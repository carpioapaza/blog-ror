  <div class="row">
    <div class="col-md-12">
      <p>Hola, actualmente hay <strong><%= pluralize(@articles.count, 'artículo') %></strong>.</p>
      <%= link_to "Crear un nuevo artículo", new_article_path, class: "btn btn-primary mb-3" %>
    </div>
  </div>

  <div class="row">
    <%= render @articles %>
  </div>
